# 🎉 MD文档自动解析系统已上线！

## ✅ 新功能说明

现在，你的**MD文档修改会自动反映到网页**上！无需手动编辑HTML文件。

## 🚀 如何使用

### 1. 编辑MD文档

直接编辑 `courses/md/` 目录下的MD文件，例如：
```bash
# 编辑C语言课程
vim courses/md/c-language.md
```

### 2. 刷新浏览器

保存MD文件后，在浏览器中刷新页面即可看到更新！

```
访问：http://localhost:8002/courses/course.html?course=c-language
```

### 3. 实时预览

修改任何内容：
- ✅ 课程概述
- ✅ 重点内容
- ✅ 代码示例
- ✅ 学习资源
- ✅ 任何其他内容

刷新页面后立即生效！

## 📝 工作原理

### 之前的方式
```
修改MD文档 → 手动编辑HTML → 上传到GitHub
```
❌ 麻烦、容易出错、需要维护两份内容

### 现在的方式
```
修改MD文档 → 刷新浏览器 → 自动显示
```
✅ 简单、实时、只需维护MD文档

### 技术实现
1. **course.html** - 通用课程页面模板
2. **course-loader.js** - JavaScript动态加载MD
3. **marked.js** - 解析MD为HTML
4. **highlight.js** - 代码高亮

## 🔗 URL格式

```
courses/course.html?course={course-name}
```

### 示例

| 课程 | MD文件 | URL |
|------|--------|-----|
| C语言 | `c-language.md` | `course.html?course=c-language` |
| 数据结构 | `data-structure.md` | `course.html?course=data-structure` |
| 数据库 | `database.md` | `course.html?course=database` |

## 📝 添加新课程

### 1. 创建MD文档

```bash
cd courses/md
cp TEMPLATE.md my-new-course.md
```

### 2. 编辑MD文档

按照模板格式填写内容，特别是Front Matter部分：

```yaml
---
title: 课程名称
category: required
hasLab: true
credit: 4
semester: 大一上
difficulty: 3
---
```

### 3. 在主页添加链接

编辑 `index.html`，添加课程卡片：

```html
<a href="courses/course.html?course=my-new-course" class="course-card-link">
    <div class="course-card" data-category="required lab">
        <div class="course-header">
            <h3 class="course-name">课程名称</h3>
            <span class="course-tag required">必修</span>
        </div>
        <!-- ... 其他内容 -->
    </div>
</a>
```

### 4. 完成！

访问页面即可看到新课程。

## ⚠️ 注意事项

### MD文件命名

- 使用小写字母和连字符
- 不要使用空格或特殊字符
- 文件名要和URL参数一致

**示例：**
- ✅ `c-language.md`
- ✅ `data-structure.md`
- ❌ `C Language.md`
- ❌ `data structure.md`

### Front Matter

Front Matter必须包含以下字段：

```yaml
---
title: 课程名称           # 必需
category: required       # 必需：required 或 elective
hasLab: true            # 必需：true 或 false
credit: 4               # 必需：数字
semester: 大一上         # 必需：学期
difficulty: 3           # 必需：1-5
author: Your Name       # 可选
lastUpdated: 2024-01-05 # 可选
---
```

### 代码高亮

使用正确的语言标识符：

````markdown
```c
// C语言代码
```

```python
# Python代码
```
````

支持的语言：`c`, `cpp`, `python`, `java`, `javascript`, `sql`, `html`, `css`, `bash`, `go`, `rust` 等。

## 🐛 常见问题

### Q: 修改MD后刷新页面看不到更新？

A: 尝试以下步骤：
1. 硬刷新浏览器（Ctrl+Shift+R 或 Cmd+Shift+R）
2. 清除浏览器缓存
3. 检查MD文件是否保存
4. 打开浏览器开发者工具查看是否有错误

### Q: 页面显示"加载失败"？

A: 检查：
1. MD文件是否存在于 `courses/md/` 目录
2. 文件名是否正确（小写、连字符）
3. Front Matter格式是否正确
4. 浏览器控制台是否有错误信息

### Q: 代码没有高亮？

A: 确保：
1. 使用了正确的语言标识符
2. 代码块格式正确（三个反引号）
3. 网络连接正常（highlight.js从CDN加载）

### Q: 可以在本地文件系统直接打开吗？

A: 不推荐。由于CORS限制，需要通过HTTP服务器访问：

```bash
python -m http.server 8002
```

## 📊 文件结构

```
courses/
├── course.html              # 通用课程页面 ⭐新增
├── c-language.html          # 旧的静态页面（可以删除）
└── md/
    ├── c-language.md        # MD文档 ⭐修改这里
    ├── data-structure.md    # 添加更多课程
    └── ...
```

## 🎯 下一步

### 立即测试

1. 打开 `courses/md/c-language.md`
2. 修改任意内容（比如改几个字）
3. 保存文件
4. 浏览器访问：`http://localhost:8002/courses/course.html?course=c-language`
5. 刷新页面 → 看到更新！

### 添加新课程

1. 复制 `courses/md/TEMPLATE.md`
2. 创建新的MD文件
3. 填写内容
4. 在主页添加链接
5. 完成！

## 🌟 优势

| 功能 | 之前 | 现在 |
|------|------|------|
| 更新内容 | 需要编辑HTML | 只需编辑MD ✅ |
| 预览 | 刷新HTML | 刷新浏览器 ✅ |
| 维护 | 两份内容 | 只需MD ✅ |
| 贡献 | 需要懂HTML | 会MD即可 ✅ |
| 代码高亮 | 手动添加 | 自动识别 ✅ |
| 版本控制 | HTML不友好 | MD完美 ✅ |

## 📚 相关文档

- [CONTRIBUTING.md](../CONTRIBUTING.md) - 贡献指南
- [courses/md/README.md](courses/md/README.md) - MD系统说明
- [courses/md/TEMPLATE.md](courses/md/TEMPLATE.md) - MD模板

---

**开始享受MD带来的便捷吧！** 🎉

修改MD，刷新页面，立刻看到效果！
